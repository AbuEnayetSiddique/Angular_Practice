import { WeightColourDirective } from './weight-colour.directive';

describe('WeightColourDirective', () => {
  it('should create an instance', () => {
    const directive = new WeightColourDirective();
    expect(directive).toBeTruthy();
  });
});
